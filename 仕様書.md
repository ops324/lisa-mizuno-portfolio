# LISA MIZUNO ポートフォリオサイト 仕様書

最終更新: 2026-09-17（rev 25 — Gallery 手前の余白を詰める）

---

## 1. 概要

| 項目 | 内容 |
|------|------|
| サイト名 | LISA MIZUNO — DJ · ARTIST |
| 対象者 | DJ / アーティスト Lisa Mizuno |
| 目的 | プロフィール・実績・リンクを一覧するポートフォリオ |
| 公開URL | https://lisa-mizuno.vercel.app |
| ホスティング | Vercel（無料プラン）+ GitHub Pages |
| アクセス制御 | 一般公開（認証なし）。Vercel・GitHub Pages とも誰でも閲覧可能 |
| CI / 品質保証 | GitHub Actions（Lint・HTML検証・シークレット走査・Lighthouse）。Biome / html-validate / gitleaks / Lighthouse CI |
| リポジトリ | GitHub: ops324/lisa-mizuno-portfolio |

---

## 2. 技術スタック

| 項目 | 内容 |
|------|------|
| 構成 | 静的サイト（HTML / CSS / JavaScript） |
| フレームワーク | なし（バニラ） |
| ビルドツール | なし（本番はソースをそのまま配信。Lighthouse CI 用に `dist/` を生成するのみ） |
| フォント | Cormorant Garamond 300/400/500（見出し・About本文英語）/ Space Grotesk（UI英語）/ Shippori Mincho 400/500（本文日本語・明朝体） |
| フォント配信 | Google Fonts |
| 外部ライブラリ | GSAP 3.12.5 + ScrollTrigger（cdnjs）、Lenis 1.1.14（jsDelivr）。いずれも SRI（`integrity` sha384 + `crossorigin="anonymous"`）付きで読み込み |
| Lint / Format | Biome 1.9.4（`biome.json`） |
| HTML 検証 | html-validate 9.5.3（`.htmlvalidate.json`） |
| パフォーマンス計測 | Lighthouse CI 0.14.0（`lighthouserc.json`） |
| パッケージ管理 | npm（`package.json` / `package-lock.json`）。実行時依存はなく、すべて devDependencies |

---

## 3. ファイル構成

```
/
├── index.html            # メインページ（全セクション）
├── style.css             # スタイルシート
├── script.js             # JavaScript
├── schedule.json         # 出演予定データ（Schedule セクションの唯一の情報源）
├── favicon.svg/.ico      # ファビコン
├── apple-touch-icon.png  # iOS ホーム画面アイコン
├── package.json          # npm スクリプト・devDependencies
├── biome.json            # Lint / Format 設定
├── .htmlvalidate.json    # HTML 検証設定
├── lighthouserc.json     # Lighthouse CI 設定
├── vercel.json           # Vercel ビルド設定（dev 依存をインストールしない）
├── .gitignore            # node_modules / .env / .DS_Store 等を除外
├── .env.example          # 管理画面の環境変数の雛形（実値はコミットしない）
├── admin/
│   ├── index.html        # Schedule 管理画面
│   └── admin.js          # 管理画面の制御
├── guide/
│   └── index.html        # 使い方ガイド（管理画面から開く。noindex）
├── api/                  # Vercel サーバーレス関数（管理画面のバックエンド）
│   ├── login.js          # ログイン / ログアウト
│   ├── schedule.js       # データ取得 / 保存
│   └── _lib/             # 共通処理（先頭 _ のためエンドポイント化されない）
│       ├── auth.js       # セッションの署名・検証
│       ├── github.js     # schedule.json の読み書き
│       └── validate.js   # 保存内容の検証
├── .github/workflows/ci.yml  # GitHub Actions CI
├── README.md             # 開発者向けドキュメント（英語）
├── 仕様書.md              # 本ファイル（プロジェクト唯一の仕様書）
└── images/
    ├── lisa-photo-web.jpg      # プロフィール写真（ヒーロー）
    ├── dj-shanghai.mp4         # Gallery Block 1 背景動画（上海公演）
    ├── dj-portrait.jpg         # 上記動画のポスター（フォールバック画像）
    ├── tokyo-node.png          # Gallery Block 2
    ├── music-artwork.png       # Connect / Music
    ├── media-editorial.png     # Connect / Media
    └── logos/                  # Works セクション掲載ロゴ
        ├── alphatheta.svg
        ├── mutek.svg
        ├── parco.svg
        ├── dommune/            # アイコン（Logo-1.png）＋ ワードマーク（Logo-2.png）
        ├── keisukeyoshida.png
        ├── shogakukan.png
        ├── shibuyanoradio.svg
        ├── ignite.svg
        ├── oizumi.png
        ├── malakai.png
        ├── tokyonode.svg
        ├── morimori.svg
        └── shibuyatv.png
```

---

## 4. ページ構成

### 4.1 ナビゲーション（`#nav`）
- 固定ヘッダー（`position: fixed`）
- ロゴ「LISA MIZUNO」（左）+ リンク「About / Works / Gallery / Connect」（右）
- **Schedule リンクは条件付き:** `#schedule` に公開中のイベントがあるときだけ `hidden` を外して「Works」と「Gallery」のあいだに現れる（→ 4.6）
- スクロール 50px 超でボーダーライン表示（`.scrolled` クラス付与）
- **スクロールスパイ:** `IntersectionObserver`（`rootMargin: '-50% 0px -50% 0px'`）で現在表示中のセクションに対応するナビリンクに `.active` クラスを付与
- **ハンバーガーメニュー（480px以下）:** `.nav-toggle`（2本線 → ×）タップでフルスクリーンオーバーレイ（`position: fixed; inset: 0`）を表示。GSAP のナビ入場アニメに `clearProps: 'transform'` を付与し、残留 transform によるオーバーレイの範囲制限（CSS containing block 問題）を防止
- Escape キー・アンカークリックでメニューを閉じる

### 4.2 ヒーロー（`#hero`）
- 全画面（`100vh`）2カラムグリッド：左＝写真・右＝名前＋肩書
- 写真：グレースケール、ホバーで部分カラー復元
- スマホ（900px以下）：縦積み（65% 写真 / 35% テキスト）
- 右下に「SCROLL」縦書きキュー（デスクトップのみ）

### 4.3 About（`#about`）
- 日英バイオグラフィー、JP / EN 切り替えトグル付き
- `.lang-toggle`（JP / EN）は「01 ABOUT」セクションラベル行の右端に配置（`margin-left: auto`）。本文はその直下から開始（名前見出しは廃止＝ナビ・ヒーロー・本文1文目で既出のため）
- 初期表示：日本語（`.bio.jp.active`）。トグルで `#bio-jp` / `#bio-en` の `.active` を切り替え
- 選択中のボタンは `aria-pressed="true"` でハイライト。切り替え後に `ScrollTrigger.refresh()` でギャラリーのスクロール位置を再同期
- **タイポグラフィ:**
  - 英語本文（`.bio.en`）は見出し名と同じ Cormorant Garamond（セリフ体）。本文可読性のため `font-size: 1.12rem` / `font-weight: 500`
  - 日本語本文（`.bio.jp`）はフォントスタック `var(--font-serif), var(--font-jp)` を使用。英字（例「LISA MIZUNO」「Resonance」）は Cormorant Garamond で、日本語は Shippori Mincho（明朝体）で表示。和文フォントはジェネリック `serif` より前に配置し、端末依存のOS明朝にならず字形が揃うようにしている
- **アクセシビリティ:**
  - bio ブロックに `lang="en"` / `lang="ja"` を付与し、スクリーンリーダーが各言語を正しく読み上げ
  - `<html lang="ja">`（初期表示は日本語のため）。言語トグル切り替え時は `script.js` の `aboutLangToggle()` が `document.documentElement.lang` を `"ja"` / `"en"` に同期する
  - 選択中の言語ボタンは色に加えて下線インジケータ（`.lang-btn[aria-pressed="true"]::after`）で明示
  - `.lang-btn:focus-visible` でキーボード操作時のフォーカスリングを表示
- セクションカウンター「01」を自動付与

### 4.4 Resonance（`#resonance`）
- About と Works の間に配置した、詩的ステートメント（イベントシリーズ名「Resonance」＝「共鳴」）の独立セクション。散文（About）→ 詩（Resonance）→ 実績（Works）という呼吸を作り、サイトの "間" として機能する
- **セクションラベル・カウンターは持たない**（瞑想的な没入演出のため、ラベルレス）。スクロール中に出会う思想セクションとしてナビにもリンクを設けていない
- **構成:**
  - `.resonance-word` … 背景的な大きな装飾文字「Resonance」（`--font-serif` イタリック、`clamp(3rem, 9vw, 6rem)`、色は `--text-sub` を `opacity: 0.22` でウォーターマーク的に薄く）。装飾要素のため `aria-hidden="true"`
  - 詩本体は About 同様 JP / EN の2ブロック（`#resonance-jp` / `#resonance-en`）。`.resonance-poem` に `.active` クラスで表示を切り替え、初期表示は日本語
  - `.resonance-title` … JP は「共鳴」（鉤括弧なし）、**勝色（かちいろ）`#55576c`**（ごく暗い紫みの青）・`1.05rem`（rev 17 で背景装飾文字「Resonance」とのバランスを見て `1.2rem` から微縮小）。サイト共通のアクセントカラー（`--accent` = 赤茶）とは別に、この見出しのみ勝色を直接指定（`--accent` 自体は変更せずナビ等への影響を避ける）。本文より詳細度の高いセレクタ（`.resonance-poem.jp p.resonance-title` 等）で JP/EN 本文ルールの上書きを防ぐ
  - **EN タイトルは非表示**（`.resonance-poem.en .resonance-title { display: none }`）。装飾文字「Resonance」が EN の見出しを兼ねるため、"Resonance" の二重表示を避ける
- **本文（原文準拠）:** JP は2連（「共鳴」の詩＋「あなたと私の過去と未来の交差点…」）、EN は1連（原文に EN 第2連が無いため）
- **言語連動:** JP / EN トグルは About のものを共用。`script.js` の `aboutLangToggle()` の `groups` 配列に `#resonance-jp` / `#resonance-en` を追加し、bio・フッター帰属表記・Resonance が1つのトグルで同時に切り替わる
- **タイポグラフィ:** 日本語（`.resonance-poem.jp`）は Shippori Mincho、英語（`.resonance-poem.en`）は Cormorant Garamond イタリック。いずれも中央寄せ・広い行間（JP `line-height: 2.4`）で詩組み。中央寄せの折り返しを均すため本文に `text-wrap: balance`、JP は単漢字の孤立を防ぐ `word-break: keep-all` を付与。さらに `overflow-wrap: anywhere` を追加し、WebKit が keep-all で読点改行しない場合の緊急折り返しを保証
- **Resonance のはみ出し対策（iOS WebKit / LINE in-app ブラウザ）:** `word-break: keep-all` はブラウザ差がある。375px の利用可能幅 327px に対し最長ハード `<br>` 行が 383px / 353px であり、Chrome は読点（、）で折り返すが **iOS Safari / WKWebView は keep-all を strict に解釈し読点改行を行わず1行を維持** → 画面外はみ出しが発生していた。スマホ幅（≤900px）では `word-break: normal` に切り替え CJK の通常折り返し（全ブラウザ共通）を有効にし、長すぎる行のみ折り返す（ハード `<br>` が主たる行構成を保持）
- **アクセシビリティ:** 各詩ブロックに `lang="ja"` / `lang="en"` を付与。フェードインは既存の `.fade-in` → `.visible`（IntersectionObserver）に乗せる
- **レスポンシブ:** デスクトップは `padding: 11rem 3rem`、モバイル（≤900px）は `padding: 7rem 1.5rem` ＋ 装飾文字の下マージン縮小＋本文を一段縮小（JP `0.92rem`/`line-height: 2.1`、EN `1.05rem`）で文字の孤立を防止。共有セクションと同じ `max-width: 860px` 中央寄せ

### 4.5 Works（`#works`）
- クライアント・出演実績ロゴ一覧
- デスクトップ：4カラムグリッド
- タブレット（900px以下）：3カラム
- スマホ（480px以下）：2カラム
- 最終アイテムが孤立する場合はグリッドで中央寄せ
- セクションカウンター「02」を自動付与

**掲載ロゴ（18社）:**
AlphaTheta（旧 Pioneer DJ）、MUTEK.JP、渋谷PARCO、DOMMUNE、KEISUKEYOSHIDA、小学館、渋谷のラジオ、IGNITE、大泉工場、MALAKAI、TOKYONODE、森ビル、シブヤテレビジョン、Qosmo、SALT GROUP、Quinto、在日ドイツ連邦共和国大使館、シマタニ昇龍工房（syouryu）

**並び順の考え方（2026-09-22 改訂 / rev 26）:**
2026-05-25 の「手書きラフ準拠」＋ブレークポイントごとに `order` を手で振る方式をやめ、**光学リズムで組んだ1本の DOM 順**に統一した。理由は3つ。

1. **見出しのないロゴウォールではカテゴリ分類は伝わらない。** 音楽／ファッション／メディアで括ると、閲覧者には分類が知覚されないまま「淡いロゴばかりの行」だけが残る（実際に試作して確認）。知覚されるのは濃淡のムラだけなので、**光学リズムを優先し、カテゴリは同点時の決め手**に留める
2. **旧並びには実測できる偏りがあった。** 各ロゴの「実インク量」（地色 `#faf8f4` からの暗さを面で積算した値）で測ると、列合計が 42 / 29 / 27 / 42 と左右端が重く中央が軽い状態で、さらに最も淡い 森ビル（3）と TOKYONODE（3）が隣接して穴になっていた。新並びは列合計 33 / 38 / 32 / 32（差 15 → 6）、軽い者どうし・重い者どうしの隣接はゼロ
3. **`order` の三重管理は事故を生む。** `order` 未指定は初期値 `0` で先頭に回り込むため、ロゴを1社足すたびに3箇所へ追記が必要だった（実際に踏んだ）。1本の並びに集約したことで **`order` 指定は全廃（34件削除）**し、この事故が構造的に消えた

**実インク量（並び順の根拠 / 1280px 実測）:**

| 重い | | 中 | | 軽い | |
|---|---|---|---|---|---|
| 大泉工場 / KEISUKEYOSHIDA | 16 | MUTEK / Qosmo | 10 | SALT | 5 |
| 渋谷のラジオ | 14 | 小学館 / Quinto | 9 | ドイツ大使館 / syouryu | 4 |
| www.Sib.tv | 11 | AlphaTheta / PARCO | 7 | IGNITE / 森ビル / TOKYONODE | 3 |
| | | DOMMUNE / MALAKAI | 6 | | |

各行に「重り」（16/16/14/11）を1つずつ置き、その周囲を中・軽で挟む。1行目は AlphaTheta・DOMMUNE・PARCO を残し、DJ としての音楽まわりの信用と知名度を冒頭で提示する。最終行はロックアップ型（紋章＋ワードマーク）の ドイツ大使館・syouryu の2社を対で置いて締める。

**DOM 順（全ブレークポイント共通）:**

| デバイス | 並び（／＝改行） |
|---|---|
| PC（4列） | AlphaTheta・KEISUKEYOSHIDA・DOMMUNE・PARCO ／ MUTEK・森ビル・渋谷のラジオ・MALAKAI ／ SALT・大泉工場・TOKYONODE・小学館 ／ www.Sib.tv・IGNITE・Quinto・Qosmo ／ ドイツ大使館・syouryu（2個中央） |
| タブレット（3列） | 上の DOM 順のまま3列で折り返し。18＝3×6 で端数なし |
| スマホ（2列） | 上の DOM 順のまま2列で折り返し。18＝2×9 で端数なし |

**最終行の端数処理:**
18社は **4列のみ 16＋2 で端数が出る**（3列・2列は割り切れる）。4列の2個余りは、半カラム（`50%`）＋半ギャップ（列ギャップ `3rem` の半分＝`1.5rem`）だけ右へずらして中央に寄せる。`transform` はレイアウトに影響しないため他の行を動かさない。

```css
.works-logo-item:nth-last-child(2):nth-child(4n + 1),
.works-logo-item:last-child:nth-child(4n + 2) {
  transform: translateX(calc(50% + 1.5rem));
}
```

**注意:** `:nth-child()` は **DOM 順で判定されカラム数を見ない**。上のセレクタは3列・2列でも同じ要素に当たってしまうため、**≤900px と ≤480px の側で必ず `transform: none` に打ち消す**こと。3列用（`3n + 1` / `3n + 2`、ずらし幅 `50% + 1rem`）と1個余り用（`grid-column`）も同様に、各ブレークポイントで「打ち消してから貼り直す」構成にしてある。社数を変えるときはこの3組を必ず見直す。

**社数と割り切れ方（今後の増減の目安）:**
4列・3列・2列すべてで割り切れるのは **12の倍数だけ**。17社は3つ全部で端数が出る最悪の数だった。18社は4列のみ端数。**16社・20社なら端数はタブレット3列のときだけ**になる。

**ロゴサイズの光学調整:**
高さ基準（`max-width: 100%; max-height: 30px`）で統一しつつ、知覚サイズ（≒面積）を揃えるため識別クラスで個別調整。
- 横長ワードマーク（MALAKAI / TOKYONODE / KEISUKEYOSHIDA / www.sib.tv / AlphaTheta / MUTEK / IGNITE）は `max-width: min(◯px, ◯%)` の組み合わせでセル端への張り出しを抑制（絶対値 px でデスクトップ上限を、`%` でモバイル列にはみ出さないことを両立）。2025-06-15 の改訂でモバイル 2 列での知覚サイズ分散が大きかったワードマーク群（AlphaTheta・IGNITE・MUTEK 等が面積最大グループを占有）に対して幅上限を絞り、**面積比を 6.4 倍 → 1.8 倍に縮小**
- 縦積み／細字ロゴは `max-height` を引き上げて存在感を確保（森ビル・小学館 38px、IGNITE 28px、DOMMUNE は icon 36px / text 40px）
- Qosmo は頭の `Q` が大きくボックス高を押し上げ、そのぶん小文字が小さく見えるため `max-height: 34px` に引き上げ（幅は `min(138px, 86%)`）。SALT（オールキャップス）は `26px`、Quinto は `24px` に抑え、いずれも他の横長ワードマークと同じ幅帯（`min(120px, 76%)` / `min(128px, 82%)`）に収めた。結果、Qosmo と Quinto の x ハイトが約 20px、SALT のキャップハイトが MALAKAI と同じ約 25px で揃う
- ドイツ大使館は「鷲＋国旗バー＋3行の英文」という横長（約 5:1）のロックアップで、英文 `of the Federal Republic of Germany`（34文字）が最小サイズになる。可読性を確保するため幅の上限だけ他ロゴより緩めた（`max-height: 34px` / `max-width: min(172px, 96%)`）
- syouryu も紋章＋細いセリフのワードマークで約 5:1。`max-height: 28px` / `max-width: min(140px, 88%)`
- 結果、モバイル 2 列でのロゴ面積の最大/最小比が 1.8 倍に収束（森ビルは縦積みロックアップのため構造上やや小さめ）

**個別実装メモ:**
- **DOMMUNE:** アイコン（`dommune/Logo-1.png`）＋ ワードマーク（`dommune/Logo-2.png`）を1セル内に横並び（`flex-direction: row`）。PNG のクリーム背景は読み込み時に canvas で透過処理（`script.js` の `removeDommuneBackground`）。グレースケールは `filter: none` で除外。モバイル（≤480px）は icon 28px / text 32px
- **IGNITE（SVG）:** `text-anchor="middle" x="123"` で 240px viewBox 内のテキストを中央寄せ
- **Qosmo（SVG）:** 公式サイト（qosmo.jp）がワードマークをインライン SVG で持っているため、そのパスをそのまま `qosmo.svg` として書き出した。先方サイトは暗背景で `fill: #fff` 指定だったので、本サイトの淡色地に合わせ他ロゴと同じ `#111111` に変更（パス形状は無改変）
- **SALT（SVG）:** 公式サイト（salt-group.jp）のヒーローがロゴを `<defs>` 内の `#logo-s` / `#logo-a` / `#logo-l` / `#logo-t` の4グループに分けて保持しているため、この4つを1つの `<g fill="#111111">` にまとめ、先方の `transform` の基準点（`translate(-111.6, -31.5)`）から逆算した `viewBox="0 0 223.22 63.15"` を与えて単独の SVG に再構成した。座標値は無改変
- **ドイツ大使館（SVG）:** 鷲（Bundesadler）と黒赤金のバーは japan.diplo.de の公式ロゴ SVG（`aamt-logo-tb-data.svg`、viewBox `0 0 54 60`）のパス・座標・指定色（`#E3001B` / `#F8D100`）をそのまま使用。英文ロックアップは先方サイトが画像ではなく HTML テキストで組んでいるため（Noto Sans 15px / 行送り 18px、バーの右 8 ユニットが余白）、同じ寸法で `Embassy` / `of the Federal Republic of Germany` / `Tokyo` の3行を Noto Sans Regular でアウトライン化して合成した。クライアント提供画像の組みと一致。**国旗バー（黒赤金）は DOMMUNE と同じく `filter: none` でグレースケールから除外し常時カラー**。グレースケールにすると金が地色（`#faf8f4`）に沈んで三色旗として判別できなくなるため。モノクロのロゴ群のなかで唯一の色味になるが、DOMMUNE に続く2例目の例外として許容している
- **syouryu（PNG）:** 公式サイトが配布しているのは**縦組み**ロゴ（`syouryu_logo_custom.png` 833×2083、昇／龍／紋章／`syouryu` の4要素が縦に並ぶ）のみで、横組みの配布データは無い。クライアント提供のプレート写真の並びに合わせ、この公式ファイルから**紋章（450×452）とワードマーク（708×114）を切り出して横に組み直した**。比率はプレート写真から実測（紋章の高さ ÷ 文字ブロックの高さ ＝ 1.77、字間 ＝ 紋章幅の 0.45 倍）。文字・紋章そのものは無改変で、色のみ他ロゴと同じ `#111111` に統一。**公式の横組みデータを受領できた場合は差し替えること**
- **Quinto（SVG）:** **公式のロゴ画像が存在しない**。先方サイト（quinto.com）はロゴを画像ではなくテキストで組んでおり、実体は Cabin Bold・`letter-spacing: 1px`（`font-size: 19px` 基準＝`0.0526em`）。そこで Google Fonts の Cabin Bold（SIL OFL 1.1）で同じ字送りを再現し、fontTools でアウトライン化して `quinto.svg` を作成した。**先方から公式ロゴデータを受領できた場合は差し替えること**

### 4.6 Schedule（`#schedule`）
- Works と Gallery のあいだに置く出演予定リスト。中身は **`schedule.json` から `script.js` が組み立てる**（HTML には空の `<ul id="sched-list">` だけを置く）
- **公開中のイベントが1件も無いあいだはセクションごと `hidden`**。ナビの「Schedule」リンクも同時に隠す。空の見出しだけが残ると放置サイトに見えるため、データが入った瞬間に初めて現れる設計。`schedule.json` の取得に失敗した場合も同様に隠したまま（「読み込めませんでした」を出さない）
- **UPCOMING / PAST 切り替え:** セクションラベル行の右端。About の言語トグル（`.lang-toggle`）と同じ作法・同じ字送りで、選択中は `aria-pressed="true"` ＋ 下線インジケータ
  - UPCOMING … 開催日が今日以降。日付の昇順（近い順）
  - PAST … 開催日が昨日以前。日付の降順（新しい順）。`.is-past` で `opacity: 0.42` に落とす
  - 全件が終了済みの場合は初期表示を PAST にする（空の UPCOMING を見せない）
  - 選んだ側に該当イベントが無いときは**一覧を空のままにする**。「現在お知らせできる予定はありません。」「過去の記録はまだありません。」のような文言は出さない（rev 24、クライアント要望）
- **行の構成:** 日付 / 本文（タイトル＋会場・都市・開始時刻）/ ラベル＋矢印 の3カラムグリッド
  - **行に罫線を引かない**。リストの上端・下端・行と行のあいだのいずれにも線を置かず、区切りは余白（`padding: 1.7rem 0`）だけで見せる（rev 24、クライアント要望）。セクションラベル下の罫線はそのまま
  - `url` を持つ行は `<a>`（別タブ）、持たない行は `<div>`。ホバーで `padding-left: 1rem` に寄せ、`↗` がフェードイン（下線のアクセント色化は罫線ごと廃止）
  - 日付表記は `2026.09.27` ＋ 曜日 `SUN`（`--font-en`、`opacity: 0.62`）
  - ラベル（`Ticket` / `Sold Out` / `Live Stream` / `Free` / `TBA` など任意文字列）は罫線囲みの小さなタグ。**`Sold Out` のみ**アクセント色（`--accent`）で表示
- **`schedule.json` の書式:**

```json
{
  "updated": "2026-09-08",
  "events": [
    {
      "date": "2026-09-27",
      "time": "22:00",
      "title": "Resonance vol.6",
      "venue": "WWW X",
      "city": "Shibuya, Tokyo",
      "url": "https://example.com/",
      "tag": "Ticket",
      "status": "published"
    }
  ]
}
```

| フィールド | 必須 | 内容 |
|-----------|------|------|
| `date` | ○ | 開催日 `YYYY-MM-DD`。この形式でないレコードは無視される |
| `title` | ○ | イベント名。空だと無視される |
| `status` | ○ | `published` のみ表示。`draft` は保存されるがサイトに出ない |
| `time` | | 開始時刻。空文字なら表示しない |
| `venue` / `city` | | 会場名 / 都市。`time` と合わせて全角ダッシュ区切りで1行に並ぶ |
| `url` | | チケット・告知ページ。**`http` / `https` 以外のスキームは捨てる**（`javascript:` 混入対策） |
| `tag` | | ラベル。空ならタグを出さない |

- **日付の判定はローカル時刻**（`toISOString()` は UTC を返すため、JST の朝9時前に当日イベントが「終了」扱いになる）
- **XSS 対策:** 行は `innerHTML` を使わず `createElement` ＋ `textContent` で組み立てる。イベント名・会場名に何が入っても文字列として扱われる
- 描画後に `ScrollTrigger.refresh()` を呼び、リストの高さ変化で下の Gallery のスクラブ位置がずれないようにする
- 日英対応は現状 **共通1つの表記**（イベント名・会場名は固有名詞が大半のため）。EN 個別表記が必要になった場合は `title_en` / `venue_en` を任意フィールドとして足せる
- **イベント画像は非対応。** Works（ロゴ）→ Schedule → Gallery（全画面動画）と画像が3ブロック連続すると Gallery の印象が弱まること、フライヤーは他者デザインで色・比率がばらつき本サイトの設定に馴染まないこと、毎回画像を用意する運用負担が増すことによる

### 4.7 Gallery（`#gallery`）
- フルブリードのダークセクション（`background: #111009`）。2ブロック構成。各ブロックは**画像（`.g-block`）とキャプション（`.g-meta`）が兄弟要素**で、キャプションは画像の**外（下）に通常フロー**で置く（展示の作法＝作品の上に文字を載せない。被写体の顔・機材にテキストやスクリムが被らない。`.g-meta` がブロック外にあるため Block 2 の clip-path/パララックスの影響も受けない）
- **手前のセクションからの暗転（グラデーション帯）と余白（rev 25）:**
  - 上端に地色→`#111009` のグラデーション帯（`#gallery::before`、ディザは `::after`）。帯の高さは PC `260px`／モバイル（≤900px）`160px`
  - `padding-top` は **帯の高さ＋`0.75rem`**（PC `17rem`／モバイル `10.75rem`）。「GALLERY」ラベルは帯が暗くなり切った直後に置く
  - `margin-top` を負にして（PC `-6rem`／モバイル `-3rem`）、**直前のセクション（Schedule。非表示時は Works）の下余白に帯の淡い頭を重ねる**。帯の上端は地色と同じなので継ぎ目は見えない。重ね幅は「最後の行から暗転が見え始めるまで」がリストの行間と同程度になる量で、前セクションの下余白（PC `9rem`／モバイル `5.5rem`）より小さく保ち本文には被せない
  - 結果、最後の予定〜「GALLERY」ラベルは PC `443px → 347px`／モバイル `304px → 236px`（他のセクション間は PC `288px`／モバイル `176px`。暗転ぶんだけ少し広い）
- **Block 1（`.g-block-1`, 全デバイス `aspect-ratio: 2.35/1`＋`cover`）:** 上海公演の背景動画 `images/dj-shanghai.mp4`
  - `<video autoplay muted loop playsinline>` による**自動再生・ループ・無音**（背景動画スタイル）
  - `poster="images/dj-portrait.jpg"` を指定（読み込み中／再生不可時のフォールバック）
  - 装飾要素のため `aria-hidden="true"`。可視キャプションは下部の `.g-meta-label`（`01 raster.focus Asia tour`）が担う
  - **サイズ＝シネスコ比で全幅（焼き込み黒帯のクロップ）:** 動画素材は上下それぞれ**約12%の黒帯が焼き込まれている**（シネスコ素材を 16:9 フレームに収めたもの）。`.g-block-1 { aspect-ratio: 2.35/1 }`＋`.g-img-1 video { object-fit: cover }` とし、コンテナ比率を“中身”の比率（≈2.35:1）に合わせることで、**黒帯だけが上下にクロップされて画面外に出る**。比率ベースなので全ビューポート共通で効き、横方向のロスや歪みもない。全幅のシネスコ帯としてリード映像を見せる。`.g-img-1` ラッパーはブロックに固定（`height: 100%`／`top: 0`）。**Block 1 はパララックスなし**（Block 2 のパララックスは維持）
  - **モバイル/タブレットの自動再生対策（`script.js`）:** HTML 属性だけだと端末の自動再生ポリシー（画面外保留・省電力モード・データセーバー・`muted` 属性の非適用）で再生されないことがあるため、JS で堅牢化：① `video.muted = true` をプロパティとして明示、② `IntersectionObserver` で画面内到達時に `play()`、③ 初回タッチ/ポインタ操作で再生するフォールバック。`prefers-reduced-motion` 時は自動再生せずポスター静止
- **Block 2（`.g-block-2`, デスクトップ `height: 75vh` ／モバイル `aspect-ratio: 3/2`）:** 静止画 `images/tokyo-node.png`（3:2 比率）
  - スクロール進入時に下から `clip-path` リビール ＋ 輝度フェード（`brightness 0.4→1`）＋ パララックス。**いずれも対象は画像（`.g-block-2`／`.g-img-2`）のみ。** キャプション `.g-meta-2` はブロック外の兄弟要素のため、クリップや移動の影響を受けない
  - キャプション：`02 攻殻機動隊展 Ghost in the Shell Collaboration REFLEX 4 - MUTEK.JP`
  - **メディアの見切れ対策:** デスクトップ（>900px）は `cover`＋クリップパス演出。主役（DJ・Pioneer DJ 機材・手元）が画像下部にあるため `.g-img-2 img { object-position: 50% 100% }`（下基準）で下を切らさず、天井/配線（雰囲気）側を多めにトリミング。モバイル（≤900px）は `aspect-ratio: 3/2; height: auto`＋`.g-img-wrap img/video { object-fit: contain }` で 3:2 画像の全体を表示（余白は `#111009` のギャラリー背景に溶ける。contain ぴったりのため object-position は無効）
- **キャプション（`.g-meta`）の組版:**
  - 構成は番号 `.g-meta-num` ＋ ラベル `.g-meta-label`（`display:flex`）。番号とタイトルの間隔は `.g-meta` の列ギャップ `gap: 0.8rem 0.9rem`（行 `0.8rem`／列 `0.9rem`、rev 18 で列を `1.4rem → 0.9rem` に詰めタイトルを番号側＝左へ寄せた。番号は左ガター基準で固定）。**棒線は廃止**（マークアップ・CSS とも撤去）。scrim 撤去に伴いラベル不透明度を `0.55 → 0.68` に微増し、平坦なダーク上での可読性を確保
  - **近接（グルーピング）:** 画像→自分のキャプションは小（`.g-meta { margin-top }` PC `1.1rem`／モバイル `0.9rem`）、キャプション→次の作品は大（`.g-block-2 { margin-top }` PC `4.5rem`／モバイル `3rem`）。**非対称の余白**で各キャプションを直上の画像に属させ、次作品の見出しへの誤読を防ぐ
  - **左揃え＋左ガター（全デバイス共通）:** 番号＋ラベルを**左揃え**。左右はサイト標準のテキストガター（PC `3rem`／モバイル `1.5rem`）。番号の左端は「GALLERY」ラベルや他セクション本文と揃う（画像は全幅だが、文字には必ず余白を持たせ画面端に張り付かせない）
  - **モバイルの改行（美術館チェックリスト式ぶら下げインデント）:** `≤900px` で `.g-meta { flex-wrap: nowrap; align-items: baseline }`＋`.g-meta-label { min-width: 0 }`。番号を単独で上に落とさず、長いタイトル（02）は番号と同じ行から始め、折り返した続き行を**タイトル左端に揃える**（番号の下に潜らせない）。これで 01・02 の改行構造を統一
- 画像／動画ラッパーは `height: 120%` / `top: -10%` でパララックス余白を確保（**Block 2 のデスクトップのみ**。Block 1 は全デバイスで `100%` / `0` に固定。モバイル ≤900px は全ラッパー `100%` / `0`）
- **モバイル（≤900px）:** GSAP パララックスと Block 2 の輝度アニメ（毎フレーム `filter` 描画が重い）を無効化。clip-path リビールとメタのフェードアップは全デバイスで実行
- セクションラベルのカウンターは付与しない（ダーク背景の没入演出のため）

**Block 2 リビールの詳細:**
- クリップ方向：`inset(0 0 100% 0)` → `inset(0 0 0% 0)`（下からクリップ＝画像上端が常に先に露出）
- トリガー：`start: 'top bottom'` / `end: 'bottom bottom'` / `scrub: 1.2`
- リビール面積＝常に可視ビューポート面積となるよう設計し、どのスクロール位置でもダーク背景が透けない
- 輝度フェード：`0.4 → 1.0`（`scrub: 2`, `end: 'top center'`、デスクトップのみ）＝暗闇から画像が立ち上がるシネマティック演出

**GSAP パララックス値（参考、デスクトップのみ）:**
Block 2 画像 `yPercent: 6`（`scrub: 1.5`）。下基準の `object-position: 50% 100%` で寄せた下部（DJ 機材・手元）が、大きいパララックスだとスクロール中に再び切れるため travel を抑制（旧 `15`→`6`）。`.g-img-wrap` / `.g-counter` に `will-change: transform` を付与。**Block 1（動画）は全体表示と両立できないためパララックスなし**

### 4.8 Connect（`#connect`）
- 外部リンク一覧（2カテゴリ：**Music** / **Media**）
- セクションカウンター「03」を自動付与
- **レイアウト:** 左右交互の2カラム行（`.connect-row` / `.connect-row--reverse`）
  - **Music 行** — 左にリンク（1fr）、右に画像（38%）：`music-artwork.png`（1:1 正方形）
  - **Media 行** — 左に画像（38%）、右にリンク（1fr）：`media-editorial.png`（3:4 縦長、`object-position: center 20%`）
  - リンク項目はホバーで右スライド（`padding-left`）＋ `↗` アイコン出現（タッチ端末では `@media (hover: none)` で矢印非表示）
  - 写真はホバー効果なし、`margin: 0 auto` で中央寄せ
  - モバイル（≤900px）：1カラムに折り返し、写真は `max-width: 58%`／（≤480px）は `max-width: 80%`

**Music カテゴリ:**
| リンク名 | URL |
|---------|-----|
| SoundCloud | https://soundcloud.com/lisamizuno |
| Resident Advisor | https://ra.co/dj/lisamizuno |
| HÖR – August 21 | https://www.youtube.com/live/NOekBA1rV5o |
| Mixmag Asia | https://mixmag.asia/read/mixmag-asia-radio-lisa-mizuno-... |

**Media カテゴリ:**
| リンク名 | URL |
|---------|-----|
| YouTube | https://youtube.com/channel/UC6mZ6mj2GKgZXIOaqlZfqBg |
| Shibuya no Radio / SOMO radio | https://note.com/shiburadi/n/nd07be32b7413 |
| 小学館 @DIME 掲載記事 ① | https://dime.jp/genre/1956242 |
| 小学館 @DIME 掲載記事 ② | https://dime.jp/genre/1956278 |

### 4.9 フッター
- `© [年] LISA MIZUNO` — 年号は JS（`new Date().getFullYear()`）で動的に書き込み。HTML フォールバック値は `2026`

---

## 5. デザイントークン

| 変数名 | 値 | 用途 |
|-------|----|------|
| `--bg` | `#faf8f4` | 背景色（温かみのあるクリーム） |
| `--text` | `#1a1714` | メインテキスト |
| `--text-sub` | `#7a6f64` | サブテキスト・ラベル（WCAG AA ≥4.5:1） |
| `--border` | `#d9d0c6` | ボーダー |
| `--font-en` | Space Grotesk, sans-serif | UI英語 |
| `--font-serif` | Cormorant Garamond, Shippori Mincho, serif | 見出し・ロゴ・About英字（和文はShippori Mincho） |
| `--font-jp` | Shippori Mincho, serif | 日本語本文 |

---

## 6. インタラクション

| 機能 | 実装 |
|------|------|
| スムーススクロール | **Lenis 1.1.14** — expo ease-out、`duration: 1.2`、`smoothTouch: false` |
| アンカーリンク | `lenis.scrollTo(target, { offset: -72, duration: 1.4 })`、クリック時にメニューを閉じる |
| スクロールアニメーション | `IntersectionObserver`（`.fade-in` → `.visible`、`threshold: 0.1`） |
| ナビボーダー表示 | `scrollY > 50` で `.scrolled` 付与 |
| スクロールスパイ | `IntersectionObserver`（`rootMargin: '-50% 0px -50% 0px'`）→ 対応ナビリンクに `.active` |
| ハンバーガーメニュー | `.nav-toggle` クリックで `#nav.menu-open` トグル、Escape / アンカーで閉じる |
| ロゴホバー | CSS `filter: grayscale(0%)` |
| Connect アイテムホバー | `padding-left` スライド + `↗` アイコン出現 |
| About 言語切り替え | JP / EN ボタンで `.bio.active`／`.resonance-poem.active`／フッター帰属表記を一括切り替え、`aria-pressed` 更新後 `ScrollTrigger.refresh()` |
| フッター年号 | JS `new Date().getFullYear()` を `#footer-year` に書き込み |
| prefers-reduced-motion | Lenis・GSAP を完全スキップ。CSS `@media` ですべてのトランジション / アニメを 0.01ms に上書き |
| no-js フォールバック | `<html class="no-js">` + インライン `<script>` でクラスを即時除去。JS 無効時は `.no-js .fade-in` で `opacity: 1; transform: none` |
| フォーカス可視化 | `a:focus-visible`、`.nav-toggle:focus-visible` に 1px solid `--text` アウトライン。`#gallery` 内は `--bg`（暗背景用） |

---

## 7. iOS Safari 対応

| 対応内容 | 実装 |
|---------|------|
| ビューポート高さ (`--vh`) | `window.innerHeight * 0.01` を CSS 変数にセット |
| `--vh` 更新タイミング | `resize` を監視するが**幅（向き）が変わった時のみ**再計算。スクロール中のアドレスバー出没による高さのみの変化は無視（ヒーロー再レイアウトによるカクつき防止） |
| スクロール復元抑止 | `history.scrollRestoration = 'manual'` + `window.scrollTo(0, 0)` |
| オーバースクロール防止 | `overscroll-behavior-y: none`（上端バウンスによる乱れを防止） |
| ナビ GPU 合成 | `transform: translateZ(0)`（`backdrop-filter` との描画競合を解消） |
| テキスト自動拡大の抑止 | `html { -webkit-text-size-adjust: 100%; text-size-adjust: 100%; }` — iOS および LINE の in-app ブラウザ（WKWebView）がテキストを自動拡大し、折り返し計算が狂って横はみ出しを悪化させる問題を防ぐ |
| 横スクロール防御ガード | `html, body { overflow-x: clip; }` — `hidden` ではなく `clip` を使用。`hidden` はスクロールコンテナを生成し `position: fixed` のナビや Lenis / ScrollTrigger を壊す恐れがあるが、`clip` はそれを回避。あわせて `img, video { max-width: 100%; }` で全画像・動画を列幅内に収束 |

---

## 8. SEO / OGP

| 項目 | 設定値 |
|------|--------|
| `<title>` | LISA MIZUNO — DJ · ARTIST |
| `description` | LISA MIZUNO is a DJ and artist based in Tokyo... |
| OG URL | https://lisa-mizuno.vercel.app/ |
| OG Image | https://lisa-mizuno.vercel.app/images/lisa-photo-web.jpg |
| Twitter Card | summary_large_image |

---

## 9. レスポンシブ対応

| ブレークポイント | 変更内容 |
|----------------|---------|
| ≤ 900px | ヒーロー縦積み / ナビパディング縮小 / Works 3カラム（`order` で並び替え）/ Schedule は3カラムのまま日付カラムを `7.6rem → 6.4rem` に詰める/ ナビ `backdrop-filter` 無効化（スクロール時のぼかし再描画コスト削減）/ ギャラリー Block2 の輝度アニメ無効化（`filter` 毎フレーム描画が重いため）/ ギャラリー手前のグラデーション帯を `160px` に短縮、重ね幅 `-3rem`（→ 4.7） |
| ≤ 480px | ナビリンク非表示・ハンバーガーメニュー表示 / Works 2カラム（`order` で並び替え）/ Schedule は1カラムに縦積み（日付→タイトル→会場→ラベル） |

---

## 10. セキュリティ・CI

### 10.1 公開ポリシー（認証なし）
- サイトは一般公開。Vercel・GitHub Pages とも誰でも閲覧でき、アクセス制限は設けていない
- 以前はプレビュー段階で Vercel Edge Middleware（`middleware.js`）による Basic 認証を掛けていたが、一般公開に伴い撤去。環境変数 `BASIC_AUTH_USER` / `BASIC_AUTH_PASS` も Vercel ダッシュボードから削除済み

### 10.2 Subresource Integrity（SRI）
- CDN から読み込む 3 スクリプト（GSAP / ScrollTrigger / Lenis）に `integrity`（sha384）＋ `crossorigin="anonymous"` ＋ `referrerpolicy="no-referrer"` を付与し、改ざん検知

### 10.3 CI（GitHub Actions `.github/workflows/ci.yml`）
`main` への push / PR で 3 ジョブを実行（`concurrency` で古い実行をキャンセル）:

| ジョブ | 内容 |
|------|------|
| Lint & HTML validate | `biome check`（JS/CSS）＋ `html-validate index.html` |
| Secret scan | `gitleaks` でコミット内のシークレット混入を走査 |
| Lighthouse CI | `dist/`（node_modules を除外して組み立て）に対し Lighthouse を 3 回実行 |

### 10.4 開発コマンド（`package.json`）
| コマンド | 内容 |
|------|------|
| `npm run lint` | Biome チェック |
| `npm run lint:fix` | Biome 自動修正 |
| `npm run format` | Biome フォーマット |
| `npm run validate:html` | html-validate |
| `npm run lighthouse` | Lighthouse CI |
| `npm run check` | lint ＋ HTML 検証をまとめて実行 |

### 10.5 Lighthouse 実測スコア（参考値）
Performance 72 / Accessibility 96 / Best Practices 100 / SEO 100（実測値。継続的に計測）

### 10.6 デプロイ・ビルド設定
- **ワークフロー:** 変更はブランチ＋PR で `main` に取り込み、マージで Vercel 本番デプロイがトリガーされる（`main` への直 push は避ける）。本番の正規ドメインは `lisa-mizuno.vercel.app`
- **Vercel ビルド（`vercel.json`）:** `installCommand` を `npm install --omit=dev` に設定。サイトは静的で実行時依存が0個（`dependencies: {}`）のため、Vercel 上では開発ツール（Biome / lhci / html-validate）を入れない。これによりビルドログの `npm warn deprecated`（間接依存 rimraf / glob / uuid 等）を解消し、ビルドも高速化。Lint / Lighthouse は GitHub Actions 側で実行するため検証は維持される
- ビルドステップは不要（純粋な HTML/CSS/JS）。GitHub Pages はミラー（静的・認証なし）

---

## 11. Schedule 管理画面（`/admin`）

水野さま本人が出演予定を追加・修正するための画面。**Vercel のサーバーレス関数から `schedule.json` を GitHub にコミットし、再デプロイで反映する**方式（→ 11.2 になぜ DB を使わないか）。

### 11.1 構成

| ファイル | 役割 |
|---------|------|
| `admin/index.html` | 管理画面のマークアップ。サイト本体の `style.css` には依存せず自己完結（本体の変更が管理画面を壊さない） |
| `admin/admin.js` | 一覧・フォーム・保存の制御。行は `createElement` ＋ `textContent` で構築 |
| `api/login.js` | `POST` ログイン / `DELETE` ログアウト |
| `api/schedule.js` | `GET` 現在のデータ取得 / `PUT` 保存 |
| `api/_lib/auth.js` | セッションの署名・検証、Cookie の読み書き |
| `api/_lib/github.js` | GitHub Contents API 経由の `schedule.json` 読み書き |
| `api/_lib/validate.js` | 保存内容の検証（サーバー側が正） |
| `.env.example` | 必要な環境変数の雛形（実値は Vercel の設定に入れる） |

`api/_lib/` のようにアンダースコアで始まるパスは Vercel がエンドポイントとして扱わないため、共通処理の置き場になる。

### 11.2 なぜ DB を使わないか
- **公開ページを静的なまま保てる。** DB 方式だと閲覧のたびに API を叩くことになり、`schedule.json` を静的配信している GitHub Pages 側のミラーが動かなくなる。この方式なら公開側の実装（→ 4.6）に一切手を入れずに済む
- **障害点を増やさない。** DB の契約・接続設定・障害対応が不要
- **履歴が git に残る。** 誤操作を戻せる
- 引き換えに**保存から反映まで 30〜60 秒**かかる（再デプロイの時間）。管理画面はその旨を保存後に明示する

### 11.3 保存の流れ
1. 管理画面が `PUT /api/schedule` に「全イベント配列」と、読み込み時の**ブロブ SHA** を送る
2. 関数が内容を検証し、`schedule.json` を GitHub Contents API でコミット（既定は `main`）
3. push を受けて Vercel が再デプロイ、CDN 上の `schedule.json` が入れ替わる
4. 公開ページが次回読み込み時に新しい内容を描画する

**SHA を往復させることが競合対策になっている。** 読み込み時点からファイルが変わっていれば GitHub 側が更新を拒否し、`409` として「別の端末から更新されています」を返す。黙って上書きしない。

なお**この方式では管理画面の保存が `main` に直接コミットされる**。コード変更は必ずブランチ＋PR を通す運用だが、`schedule.json` は掲載内容そのもの（＝公開操作）なので例外扱いとする。

### 11.4 認証
- パスワード1つ（`ADMIN_PASSWORD`）。照合は **SHA-256 ダイジェスト同士の `timingSafeEqual`**（長さの違いで例外を出さず、実行時間から内容を推測させない）
- 成功すると `SESSION_SECRET` で HMAC 署名したトークンを Cookie に発行。**`HttpOnly` / `Secure` / `SameSite=Strict` / 有効期限8時間**。サーバー側に状態を持たないため、期限切れ以外の失効管理は不要
- ペイロードは有効期限のみ。個人情報も権限情報も入れない
- **環境変数が欠けている場合はフェイルクローズ**（`503`）。空文字の秘密鍵に何でも一致してしまう事故を防ぐ
- `GET /api/schedule` にも認証を要求する。下書きイベントを外に出さないため

### 11.5 保存内容の検証（サーバー側）
管理画面側の入力チェックは利便性のためのもので、**正はサーバー側**。既知のフィールドだけを通し、それ以外は捨てる。

| 項目 | 制約 |
|------|------|
| 件数 | 最大 300 件 |
| `date` | `YYYY-MM-DD`。**実在する日付か検証**（`2026-02-31` は弾く） |
| `time` | `HH:MM`（24時間制）または空 |
| `title` | 必須・最大 200 文字 |
| `venue` / `city` | 最大 120 文字 |
| `tag` | 最大 40 文字 |
| `url` | 最大 500 文字。**`http` / `https` 以外のスキームは拒否**（公開側の描画と同じ規則） |
| `status` | `published` / `draft` 以外は `draft` に倒す |

コミットする JSON は **2スペースインデント＋末尾改行**で書き出す。Biome の整形結果と一致させないと、管理画面の保存が CI の Lint を落とすため。

### 11.6 環境変数（Vercel に設定）

| 変数 | 内容 |
|------|------|
| `ADMIN_PASSWORD` | ログインパスワード。20文字以上のランダム文字列 |
| `SESSION_SECRET` | Cookie 署名鍵。32バイト以上（`openssl rand -base64 32`）。変更すると全セッションが無効になる |
| `GITHUB_REPO` | `ops324/lisa-mizuno-portfolio` |
| `GITHUB_BRANCH` | `main` |
| `GITHUB_TOKEN` | fine-grained PAT。**対象リポジトリを1つに絞り、権限は Contents: Read and write のみ** |

**トークンの有効期限は最長（1年）で発行する運用とする。** 期限が切れると管理画面の保存だけが失敗する（公開ページは静的配信のため影響しない）。切れた場合は新しいトークンを発行して `GITHUB_TOKEN` の値を差し替えるだけで、コードの変更は不要。期限切れは → 11.10 の通り専用のメッセージで案内する。

### 11.7 使い方ガイド（`/guide/`）
水野さま向けの操作説明を**サイト本体に置き、管理画面から開けるようにする**。メールや PDF を探し出さないと手順が分からない状態にしない。困ったその場で開けることが要件。

- `guide/index.html`（`noindex, nofollow`）。単体で完結し、サイト本体の `style.css` にも管理画面にも依存しない
- 管理画面の**ヘッダー**と**ログイン画面**の両方にリンクを置く。ログインできないときこそ手引きが要るため、ログイン前から読める位置にも置く
- リンクは `../guide/` と相対で書く（ルート絶対パスだとサブパス配信で壊れる）
- パスワードは記載しない。URL を知られても実害がないようにしておき、更新画面自体はパスワードで守る
- 内容は 11.2〜11.6 の挙動と一致させる。**実装を変えたらガイドも直す**

### 11.8 保存が2段階であることの明示
編集は「一覧を直す」と「サイトに公開する」の2段階になっている。1段階目で完了したと誤解されると、**書いたのに公開されない**という最悪の失敗になるため、UI 側で繰り返し明示する。

- フォームのボタンは **「一覧に追加」/「変更を反映」**（新規・編集で出し分け）。旧「この内容で確定」は保存が済んだように読めたため改めた
- 追加・変更・削除のたびに琥珀色の注意書きを出す：**「〜しました。まだ公開されていません。サイトに反映するには、画面下の『保存して公開』を押してください。」**。赤（エラー）でも緑（完了）でもない色で「手が必要」だけを意味させる
- 未保存のあいだは保存バーの枠・背景をアクセント色に変え、文言も「未保存の変更があります。右のボタンで公開してください」にする
- ヘッダーの状態表示も「未保存の変更あり」に変わる
- 未保存のままページを離れようとすると確認を出す（`beforeunload`）

この設計は、最初の実運用テストで制作者自身が1段階目で止まったことを受けて入れたもの。**初見の利用者は確実に同じところで止まる**という前提で組んでいる。

### 11.9 ブラウザのダイアログに依存しない
**`window.confirm()` / `window.alert()` は使わない。** ブラウザの設定やコンテンツブロッカーに抑制されることがあり、抑制された `confirm()` は**即座に `false` を返す**。コードは「利用者がキャンセルした」と判断して黙って終了するため、画面上は**押しても何も起きない**という、故障と区別のつかない状態になる。

実際に、削除ボタンが無反応という事象が実運用テストで発生した（原因はこれ）。同じ理由でログアウトも、未保存の変更がある状態では無反応になっていた。

- **削除**は画面内の2段階確認（「このイベントを削除」→「一覧から削除しますか？ 削除する／やめる」）。フォームを開閉するたびに確認状態は戻す
- **ログアウト**は未保存時に1度だけ警告を出し、ボタン自身の文言を「破棄してログアウト」に変えて2度目の押下で実行する
- `beforeunload` の離脱警告はブラウザ標準のまま（抑制されても実害がない保険のため）

あわせて、**利用者の操作に対して黙って `return` しない**。対象が選ばれていない等の異常時も必ず画面にメッセージを出す。無反応は利用者にとって故障と同じ。

### 11.10 失敗時のメッセージ
GitHub 側の失敗は原因ごとに意味が違うため、一律の「時間をおいてお試しください」にしない。**とくにトークンの期限切れは待っても直らない**ので、待てば直るかのような案内は有害。

| GitHub の応答 | 判定 | 管理画面の表示 |
|--------------|------|--------------|
| `401` | 期限切れ・無効 | GitHub トークンの有効期限が切れているか、無効になっている可能性があります。管理者にご連絡ください。 |
| `403` / `429`（残数0） | 利用制限 | GitHub の利用制限に達しました。しばらく待ってからお試しください。 |
| `403`（残数あり） | 権限不足 | GitHub トークンの権限が不足している可能性があります（Contents: Read and write が必要です）。管理者にご連絡ください。 |
| `404` | 対象が見つからない | 保存先のファイルが見つかりません。GitHub トークンの対象リポジトリ設定をご確認ください。管理者にご連絡ください。 |
| `409` / `422` | 競合 | 別の端末から更新されています。画面を再読み込みしてからやり直してください。 |
| その他 | 不明 | 時間をおいてお試しください。 |

- 文言は操作に応じて「保存できませんでした」「読み込みできませんでした」を前置きする
- **fine-grained トークンがリポジトリへのアクセスを失うと `403` ではなく `404` が返る**ため、「ファイルが無い」と「アクセス権が外れた」は同じ文言で案内する
- **読み込みに失敗した場合はログイン画面ではなく専用の画面**（`#fatal-view`）を出す。ログイン済みなのにパスワード欄を見せると、入力の誤りだと誤解させるため
- 上流のエラーメッセージはそのまま返さない（リポジトリ名などが露出しうる）

### 11.11 既知の制限
- **総当たり対策は失敗時の一律 400ms 遅延のみ。** サーバーレス関数はインスタンスをまたいで状態を共有しないため、回数制限を正しく実装するには外部ストアが要る。パスワードを十分長くすることで補う前提
- **GitHub Pages 側のミラーでは管理画面は動かない**（サーバーレス関数が無いため）。公開ページの表示には影響しない
- 管理画面は `noindex, nofollow`。検索結果には出ない

---

## 12. 変更履歴

| 日付 | 内容 |
|------|------|
| 2026-09-17 | **Gallery 手前の余白を詰める（rev 25、PR #52）**（→ 4.7）：クライアント指摘「Schedule の最後の予定から GALLERY までの余白が間延びしている」への対応。モバイルで最後の予定〜「GALLERY」ラベルが `304px` あり、他のセクション間（`176px`）の約1.7倍だった。原因は Schedule の下余白（`5.5rem`）のあとに、Gallery の上余白（`12rem`）の中のグラデーション帯（`180px`）が続き、「地色の余白」と「帯の淡い部分」が重なって空白に見えていたこと。① `#gallery` に負の `margin-top`（PC `-6rem`／モバイル `-3rem`）を付け、帯の淡い頭を前セクションの下余白に重ねた。帯の上端は地色と同じなので継ぎ目は出ない。重ね幅は「最後の行から暗転が見え始めるまで」がリストの行間と同程度になる量。② モバイルは帯を `180px → 160px` に短縮し、`padding-top` を `12rem → 10.75rem`（帯＋`0.75rem`、PC の `260px`＋`0.75rem`＝`17rem` と同じ関係）にした。帯の階調（11段の smoothstep）とディザは変更なし。③ PC も同じ構造で `443px`（他のセクション間 `288px`）空いていたため同じ考え方で揃え `347px` に。帯の高さは据え置き。結果はモバイル `304px → 236px`、PC `443px → 347px`。Schedule 非表示（Works → Gallery）のときも同じ規則が効く。重ねた分は前セクションの下余白の内側に収まり、フェードイン中（`translateY(16px)`）でも本文とは最小 `24px` 離れる。`style.css` のみ変更 |
| 2026-09-15 | **Schedule の罫線と「予定なし」文言を削除（rev 24、PR #51）**（→ 4.6）：クライアント要望。① イベント行の罫線をすべて削除（リスト上端・下端・行と行のあいだ）。区切りは既存の上下余白だけで見せる。線そのものを無くす要望のため、ホバー時に下線を `--accent` に変える演出も廃止し、左寄せと `↗` のフェードインのみ残した。② UPCOMING / PAST で該当イベントが無いときの文言（「現在お知らせできる予定はありません。」「過去の記録はまだありません。」）を表示しないようにし、一覧は空のままとした。使われなくなった `.sched-empty` のスタイルも削除。セクションラベル下の罫線、ラベルタグの枠線、公開イベント0件でセクションごと隠す挙動は変更なし。Vercel プレビューでクライアント承認取得済み。`style.css` / `script.js` を変更 |
| 2026-09-12 | **使い方ガイド `/guide/` を追加（rev 23）**（→ 11.7）：水野さま向けの操作説明をサイト本体に設置し、**管理画面のヘッダーとログイン画面の両方からリンク**した。メールや PDF を探さないと手順が分からない状態を避け、困ったその場で開けるようにするのが狙い。ログインできないときこそ手引きが要るため、ログイン前から読める位置にも置いている。`noindex, nofollow` 付与、パスワードは非掲載（URL を知られても実害がない状態にし、更新画面自体はパスワードで守る）。リンクは `../guide/` と相対で記述（ルート絶対パスだとサブパス配信で壊れるため）。内容は UPCOMING / PAST の切り替え、当日の扱い、中止・延期時は削除せず下書きに戻す運用まで含む。公開側・API には変更なし（`guide/` を追加し `admin/index.html` にリンクを足したのみ） |
| 2026-09-12 | **管理画面：保存が2段階であることを明示（rev 22）**（→ 11.7）：実運用テストで、フォームの「この内容で確定」を押した時点で保存が済んだと誤解され、実際には GitHub に何も届いていないという事象が起きた。**書いたのに公開されない**は最悪の失敗なので、UI 側で繰り返し明示する。① フォームのボタンを **「一覧に追加」/「変更を反映」**（新規・編集で出し分け）に改称。② 追加・変更・削除のたびに琥珀色で「まだ公開されていません。画面下の『保存して公開』を押してください」と案内。赤（エラー）でも緑（完了）でもない色で「手が必要」だけを意味させる。③ 未保存のあいだ保存バーの枠・背景をアクセント色に変え、文言も行動を促す形に変更。④ **`window.confirm()` への依存を撤廃**（→ 11.8）。実運用テストで削除ボタンが無反応になる事象が発生。原因は、ブラウザ側でダイアログが抑制されると `confirm()` が即座に `false` を返し、コードが「キャンセルされた」と判断して黙って終了していたこと。同じ理由でログアウトも未保存時に無反応だった。削除は画面内の2段階確認に、ログアウトはボタン文言を変える2度押しに置き換えた。あわせて、利用者の操作に対して黙って `return` せず必ずメッセージを出す方針にした。公開側・API には変更なし（`admin/index.html`・`admin/admin.js` のみ） |
| 2026-09-08 | **Schedule 管理画面（`/admin`）を追加（rev 21）**：水野さま本人が出演予定を更新できる管理画面と、そのバックエンドを追加（→ 11章）。**既存ファイルには一切手を入れていない純粋な追加**（`index.html`/`style.css`/`script.js`/`schedule.json` は無変更）。① **方式は Vercel 関数 + GitHub コミット。** 保存すると関数が `schedule.json` をリポジトリにコミットし、再デプロイで反映される（30〜60秒）。DB を使わないのは、公開ページを静的配信のまま保てる（GitHub Pages ミラーも動く／公開側の実装を触らずに済む）、障害点を増やさない、履歴が git に残る、の3点による。② **競合対策:** 読み込み時のブロブ SHA を保存時に往復させ、その間にファイルが変わっていれば GitHub が更新を拒否し `409` を返す。黙って上書きしない。③ **認証:** パスワード1つを SHA-256 ダイジェスト同士の `timingSafeEqual` で照合し、HMAC 署名した8時間有効の `HttpOnly`/`Secure`/`SameSite=Strict` Cookie を発行。サーバー側に状態を持たない。環境変数が欠けていればフェイルクローズ（`503`）。下書きを漏らさないため `GET` にも認証を要求。④ **検証はサーバー側が正。** 既知フィールドのみ通し、実在しない日付（`2026-02-31` 等）・不正な時刻・`http(s)` 以外の URL・長すぎる文字列・300件超を拒否。コミットする JSON は Biome の整形と一致させ（2スペース＋末尾改行）、管理画面の保存が CI を落とさないようにした。⑤ **既知の制限:** 総当たり対策は失敗時の一律 400ms 遅延のみ（サーバーレスはインスタンス間で状態を共有しないため）。パスワードを長くすることで補う前提。GitHub Pages 側では管理画面は動かない（公開ページの表示には影響なし）。⑥ 認証・検証・エラー分類のロジックは 48 ケースのテストで確認（署名検証・改竄・期限切れ・長さ違いの比較・Cookie 解析・各種不正入力）。⑦ **失敗時のメッセージを原因ごとに出し分ける**（→ 11.7）。GitHub の `401`（期限切れ・無効）／`403`＋残数0（利用制限）／`403`（権限不足）／`404`（対象なし・アクセス喪失）／`409`・`422`（競合）を判別し、それぞれ別の案内を出す。**トークンの期限切れは待っても直らない**ため「時間をおいてお試しください」を出さない。読み込み失敗時はログイン画面ではなく専用画面を出す（ログイン済みなのにパスワード欄を見せると入力ミスと誤解させるため）。`admin/`・`api/`・`.env.example` を追加 |
| 2026-09-08 | **Schedule（活動スケジュール）セクションを追加（rev 20）**：Works と Gallery のあいだに出演予定リストを新設。データは新規の `schedule.json` を唯一の情報源とし、`script.js` の `schedule()` モジュールが取得して行を組み立てる（HTML には空の `<ul>` のみ）。① **公開中のイベントが0件のあいだはセクションごと `hidden`**、ナビの Schedule リンクも隠す。取得失敗時も同じく隠したまま（空の見出しや「読み込めませんでした」を出さない）。② UPCOMING / PAST 切り替えを About の言語トグルと同じ作法でラベル行右端に配置。UPCOMING は日付昇順、PAST は降順で `opacity: 0.42`。全件終了済みなら初期表示を PAST にする。③ 行は Connect の `.connect-item` と同じホバー挙動（左寄せ＋下線アクセント＋`↗`）。`url` を持つ行のみ `<a>` 化。④ **セキュリティ:** 行は `innerHTML` を使わず `createElement` ＋ `textContent` で構築、`url` は `http`/`https` 以外のスキームを破棄。⑤ **日付判定はローカル時刻**（`toISOString()` の UTC 換算だと JST 朝9時前に当日イベントが終了扱いになる）。⑥ 描画後 `ScrollTrigger.refresh()` でリスト高さの変化を下の Gallery のスクラブに反映。⑦ レスポンシブは既存ブレークポイントに追従（≤900px で日付カラムを詰め、≤480px で1カラムに積む）。⑧ イベント画像は非対応とした（Works→Schedule→Gallery と画像が連続して Gallery の印象が弱まる／フライヤーは他者デザインで統一が効かない／更新負担が増す）。日英はイベント名・会場名が固有名詞中心のため共通1表記。実装前にクライアント向けプレビュー（PR #45）で承認取得済み。`index.html` / `style.css` / `script.js` / `schedule.json` を変更 |
| 2026-06-23 | **読み込み時の白フラッシュ／ヒーロー点滅を解消（rev 19）**：本番リンクを開く（再読込）たびに、ページ描画直前に画面が白→生成り→コンテンツと明度ジャンプし「パッパッ」とちらつく現象を修正（30fps フレーム解析で blank フレーム＝純白 `#fefefe`、描画後＝生成り `#faf8f4` を確認）。① **白フラッシュ（主因）**：背景色 `--bg: #faf8f4` は外部 `style.css`（レンダーブロッキング）にしか無く、届くまでブラウザが既定の白を描画していた。`<head>` 内・外部CSS読込より前にインライン `<style>html{background-color:#faf8f4}</style>` を先出しし、最初の描画から生成り色を適用。② **ヒーローの「表示→消失→フェード」点滅（副因）**：`#nav`/`.hero-image`/`.hero-name`/`.hero-title` は GSAP `.from()` 入場だが初期 `opacity:0` が無く、GSAP実行前に一瞬フル表示されていた。`.fade-in` と同方針で `@media (prefers-reduced-motion: no-preference)` ＋ `html:not(.no-js)` スコープの初期 `opacity:0` を CSS に追加（JS無効・低モーション時は隠さない）。③ ②の副作用対策：GSAP `.from()` は要素の現在 opacity を終了値として読むため、CSSで0にするとヒーローが0→0で消えたままになる。タイムライン前に `gsap.set('#nav, .hero-image, .hero-name, .hero-title', { opacity: 1 })` を挿入し終了値を1へ固定。④ 保険：GSAP CDN 読込失敗・低モーション時にヒーローが隠れたまま残らないよう、`script.js` の入場分岐を `else` フォールバック化し当該要素の `opacity` を明示的に1へ戻す。見た目・タイポ・既存アニメは不変。`index.html`/`style.css`/`script.js` を変更 |
| 2026-06-21 | **Gallery キャプションのタイトルを左へ寄せる（rev 18）**：`.g-meta`（flexコンテナ）の `gap` を `0.8rem 1.4rem` → `0.8rem 0.9rem` に変更。第2値（列ギャップ）のみ `1.4rem → 0.9rem` に詰め、番号 `.g-meta-num`（左ガター基準で固定）はそのまま、作品タイトル `.g-meta-label` を約 `0.5rem`（8px）番号側＝左へ寄せて番号とタイトルのまとまりを締めた。列ギャップは基本 `.g-meta` ルールのみで定義（モバイル上書きは `gap` 不変）のため PC・モバイル共通で反映。番号位置・左右ガター・フォント・不透明度は据え置き。HTML/JS 変更なし |
| 2026-06-21 | **Resonance 見出し「共鳴」を微縮小（rev 17）**：`.resonance-title` の `font-size` を `1.2rem` → `1.05rem`（約12%縮小）。背景装飾文字「Resonance」と詩本文との間で見出しが大きすぎず収まるようバランス調整。色（勝色 `#55576c`）・字間・行間・下マージン等その他プロパティは据え置き。EN タイトルは引き続き非表示。HTML/JS 変更なし |
| 2026-06-16 | **Resonance 見出し「共鳴」を勝色に変更（rev 16）**：`.resonance-title` の色を共通アクセントカラー（`--accent` = `#7d3531` 赤茶）から **勝色（かちいろ）`#55576c`**（ごく暗い紫みの青／武士が「勝つ色」として縁起を担いだ伝統色）へ変更。`--accent` 変数自体は変更せず、当該セレクタにのみ直接指定することでナビ等サイト共通のアクセント表現には影響を与えない。HTML/JS 変更なし |
| 2026-06-20 | **Gallery キャプションを画像の外（下）へ刷新 ＋ Block 1 の焼き込み黒帯クロップ（rev 16、PR #42）**：① キャプション `.g-meta` をブロック内オーバーレイ（`position:absolute; bottom` ＋ scrim）から**画像の下の兄弟要素（通常フロー）**へ移動。作品（顔・機材）に文字・黒幕を被せない展示の作法に。scrim（`.g-meta::before`）・01 の持ち上げ（`.g-meta-1` の `bottom`/`::before`）を撤去、ラベル不透明度を `0.55 → 0.68` に微増。② キャプションがブロック外になったことで Block 2 の clip-path/パララックスはキャプションに掛からない。③ Block 1 動画は素材に上下それぞれ約12%の黒帯が焼き込まれているため、`.g-block-1` を `aspect-ratio: 2.35/1` ＋ `cover`（コンテナ＝中身のシネスコ比）にして黒帯を上下クロップ（全ビューポート共通、旧 `82vh`/`16:9`+`contain` を置換）。④ 近接（非対称マージン：画像→キャプション小／次作品へ大）で各キャプションを直上画像にグルーピング。⑤ キャプション組版：**棒線を廃止し全デバイス左揃え**（左右はサイト標準ガター PC `3rem`/モバイル `1.5rem`、番号左端は「GALLERY」ラベルと整列）。モバイルは美術館チェックリスト式の**ぶら下げインデント**（`flex-wrap:nowrap; align-items:baseline` ＋ `.g-meta-label { min-width:0 }`）で 01/02 の改行構造を統一（番号を単独で上に落とさず、長題は続き行をタイトル左端に揃える）。⑥ Block 2 画像の下見切れ対策：主役（DJ・Pioneer DJ 機材・手元）が下部にあるため `.g-img-2 img { object-position: 50% 100% }`（下基準）にし、さらにパララックスで下が再び切れないよう `yPercent: 15→6` に抑制（`script.js`） |
| 2026-06-15 | **Resonance のはみ出し解消・横スクロール防止・デザイン微調整（rev 15、PR #40）**：① iPhone 12 mini の LINE in-app ブラウザ（iOS WebKit）で Resonance テキストが画面外へはみ出す実バグを修正。`word-break: keep-all` の WebKit strict 挙動が原因（Chrome は読点折り返しあり、WebKit はなし）。スマホ幅（≤900px）で `word-break: normal` に切り替え全ブラウザで確実に折り返す。基本ルールに `overflow-wrap: anywhere` を追加し keep-all/normal 双方の安全網とした。② 横スクロール再発防止ガード：`html, body { overflow-x: clip }`（`hidden` でなく `clip` で fixed/Lenis を壊さない）・`img, video { max-width: 100% }`・`-webkit-text-size-adjust: 100%`（iOS/in-app のテキスト自動拡大抑止）。③ `<html lang>` を `en` → `ja` に修正し、言語トグルで `document.documentElement.lang` を `ja`/`en` に同期（a11y/SEO）。④ Works ロゴの `max-width` を `% + px` の組み合わせに改訂しモバイル 2 列での知覚サイズ分散を縮小（面積比 **6.4 倍 → 1.8 倍**）。⑤ Gallery Block 2 をモバイルで `aspect-ratio: 3/2; height: auto` に変更し、3:2 画像の `contain` 表示時に生じていた上下 **~246px の暗い letterbox 余白を解消**しキャプションを画像に密着させた |
| 2026-06-15 | **Gallery Block 1 のモバイル余白・PC キャプション位置・01↔02 ガターを刷新（rev 14）**：① モバイル（≤900px）の `.g-block-1` を `80vh`/`70vh` 縦長固定から `aspect-ratio: 16/9` + `height: auto` に変更し、`contain` の上下レターボックス（進入直後の黒余白）と浮いたキャプションを解消。② PC（>900px）は `aspect-ratio: 16/9` だと横長画面で高さがビューポートを超え `01` キャプションが画面外に落ちていたため、Block 2 同様 `height: 82vh` + `object-fit: cover`（全幅維持）に変更し、キャプションを常に画面内へ。③ Block 1 に `margin-bottom`（PC `3rem`/モバイル `2rem`）のダークガターを追加し 01・02 を分離。④ 境目に張り付いて「動画と余白の両方に触れる中途半端さ」を解消するため `.g-meta-1` を持ち上げ（PC `bottom: 2.5rem`/モバイル `1rem`）、01 のスクリムを短縮・軽量化（`155px`/`95px`, 最大 `0.8`）して黒の量を低減。02 は最終ブロックのため不変。JS 変更なし |
| 2026-06-15 | **Gallery Block 1 を PC でも左右いっぱい表示（rev 13）**：固定高さ `90vh` のままだとウィンドウが 16:9 より横長のとき `object-fit: contain` の余白が左右に出ていた（モバイルは横端まで届くのに PC は左右が空く差）。`.g-block-1` を `aspect-ratio: 16/9` に変更し、コンテナを動画と同比率に。余白は上下のみになり左右端まで表示（横長×低めウィンドウで片側 ~265px → ~1px）。モバイルは明示 `height` 優先で従来どおり。JS 変更なし（Block 1 は元々パララックス対象外） |
| 2026-06-13 | **Gallery Block 1 動画を PC でも全体表示に（見切れ修正、PR #37）**：パララックス用ラッパー（120%/top -10%）＋`cover` で PC でも左右が切れていたため、`.g-img-1` のラッパーをブロックに固定（`100%`/`0`）し `object-fit: contain` 化。両立できない Block 1 のパララックスを除去（Block 2 は維持）。1440幅=帯なし全体表示、横長画面=左右黒帯付き全体表示 |
| 2026-06-13 | **Gallery のモバイル/タブレット見切れ対策**：≤900px で `.g-img-wrap img/video` を `object-fit: contain` に切替え、横長素材（16:9 動画・3:2 画像）の全体を表示（高さは維持、余白は `#111009` 背景に溶ける黒帯）。デスクトップは従来どおり `cover` |
| 2026-06-13 | **Resonance のデザイン仕上げ**：EN タイトルを非表示にし装飾文字との "Resonance" 二重表示を解消／装飾文字を `--text-sub` opacity 0.22 で視認性改善／モバイルの折り返し最適化（`text-wrap: balance`・JP `word-break: keep-all`・本文一段縮小）／JP タイトルを「共鳴」（鉤括弧除去）・`1.2rem` に調整。本文は原文準拠（JP 2連・EN 1連） |
| 2026-06-13 | About と Works の間に **Resonance**（共鳴）セクションを追加。イベントシリーズ名を詩的ステートメントとして提示する独立セクション（ラベル・カウンターなし、ナビ非掲載）。背景の装飾文字「Resonance」＋ JP/EN 詩本体で構成し、About の言語トグルに連動（`aboutLangToggle()` の `groups` に `#resonance-jp`/`#resonance-en` を追加）。タイトルは本文ルールの上書きを避けるため詳細度を引き上げ |
| 2026-06-03 | Works 1番目のロゴを Pioneer DJ から **AlphaTheta**（親会社・新ブランド）の公式ワードマークに差し替え（`images/logos/alphatheta.svg`、CSS クラス `--alphatheta`、alt も `AlphaTheta`）。あわせて DOMMUNE ロゴを光学拡大（icon 30→36px / text 34→40px、モバイル 24→28px / 28→32px）し、ワードマークの存在感を MUTEK・PARCO と整合させた |
| 2026-06-03 | サイトを一般公開化。Basic 認証を撤去（`middleware.js` / `.env.example` を削除し、関連ドキュメントを更新）。Vercel の環境変数 `BASIC_AUTH_USER` / `BASIC_AUTH_PASS` も削除 |
| 2026-06-01 | モバイル/タブレットで Gallery Block 1 の背景動画が自動再生されない問題に対処（PR #29）。`script.js` に muted プロパティ明示・IntersectionObserver 再生・初回操作フォールバックを追加 |
| 2026-06-01 | `vercel.json` を追加し `installCommand` を `npm install --omit=dev` に設定。Vercel ビルドログの deprecation 警告を解消・ビルド高速化（PR #27）。あわせて英語版 SPEC.md を廃止し仕様書を `仕様書.md` 一本に統合（PR #26） |
| 2026-06-01 | Basic 認証の環境変数参照をハンドラ内（リクエスト時）へ移動し、本番ログイン失敗を解消（PR #24） |
| 2026-06-01 | セキュリティ／品質基盤を整備（PR #23）: Basic 認証の資格情報を環境変数化（直書き廃止・フェイルクローズ・定数時間比較）、CDN スクリプトに SRI 付与、`.gitignore` / `.env.example` 追加、Biome・html-validate・Lighthouse CI 導入、GitHub Actions CI（Lint／gitleaks／Lighthouse）追加、README 整備 |
| 2026-06-01 | Gallery Block 1 を静止画 `dj-portrait.jpg` から上海公演の動画 `dj-shanghai.mp4` に差し替え（自動再生・ループ・無音の背景動画スタイル、`poster` にフォールバック画像、`aria-hidden`）。ファイル名は配信安定性のため ASCII へリネーム |
| 2026-05-25 | 日本語フォントを Noto Sans JP（ゴシック）→ Shippori Mincho（明朝）に変更し、Cormorant Garamond と調和する上品な印象に。あわせて和文フォントをジェネリック `serif` より前に配置し、従来「`serif` が先に当たり端末依存のOS明朝になる」不具合を解消。フッターの帰属表記JPも Shippori Mincho に統一 |
| 2026-05-25 | Works ロゴ配置をデバイス別に再設計（手書きラフ準拠）。各ロゴに識別クラスを付与し DOM 順を PC 順に固定、タブレット／スマホは CSS `order` で並び替え。ロゴサイズを高さ基準＋面積で光学的に均一化（横長ワードマークに `max-width: min()` 上限、縦積み／細字ロゴの高さ引き上げ）。DOMMUNE（icon 30px / text 34px）と IGNITE（32px）を要望により拡大。 |
| 2026-05-25 | UI 全体改善（rev 7）: モバイルハンバーガーメニュー追加（480px以下、フルスクリーンオーバーレイ）、スクロールスパイ（ナビ現在地ハイライト）、`prefers-reduced-motion` 対応（Lenis/GSAP スキップ + CSS）、no-js フォールバック、フォーカス可視化統一（`focus-visible`）、フッター年号を JS 動的化、ギャラリーキャプション誤字修正（「Ghost and」→「Ghost in」）。GSAP nav 入場に `clearProps: 'transform'` を追加し、モバイルメニューが viewport 全体を覆わない問題を解消。 |
| 2026-05-25 | About 冒頭の見出し名「Lisa Mizuno」を削除し、JP/EN トグルを「01 ABOUT」ラベル行へ移動。モバイルのスクロールカクつき対策（`--vh` を幅変化時のみ再計算、ナビ `backdrop-filter` をモバイルで無効化、ギャラリー輝度アニメをデスクトップ限定に） |
| 2026-05-24 | About 英語本文を Cormorant Garamond 化（1.12rem/500）、日本語本文内の英字も Cormorant Garamond 化。アクセシビリティ改善（bio に lang 属性、`--text-sub` コントラスト #8a7f76→#7a6f64、トグル下線、focus-visible）。Vercel に Basic Auth ミドルウェア追加 |
| 2026-05-20 | About セクションに日英切り替えトグル（JP / EN）を追加 |
| 2026-05-17 | Vercel デプロイ、iOS Safari スクロール上部グリッチ修正（`overscroll-behavior-y`、`orientationchange`、nav GPU 合成） |
| 2026-05-16 | セクションカウンター追加、connect 矢印追加、works 孤立アイテム修正 |
| 2026-05-15 | フォント統一修正（connect-name JP フォールバック、section-label sans-serif、bio ウェイト） |
| 2026-05-10 | 初版リリース（GitHub Pages） |
