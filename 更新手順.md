# LISA MIZUNO サイト 更新手順書

このサイトは HTML / CSS / JavaScript のみで作られた静的サイトです（ビルド不要）。
`main` ブランチに変更を反映（push）すると、自動で本番（GitHub Pages / Vercel）に公開されます。

主に編集するファイルは `index.html`（内容）と `style.css`（見た目）の2つです。

---

## 1. WORKS（実績ロゴ）を追加・変更する

### 画像を置く
1. ロゴ画像を `images/logos/` フォルダに入れる（推奨：SVG。なければ背景透過PNG）
2. ファイル名は半角英数（例：`newclient.svg`）

### HTMLに追加する（`index.html` の `<div class="works-grid ...">` 内）
既存のロゴ1つ分をコピーして、画像パスと識別クラスを変えるだけです。

```html
<div class="works-logo-item works-logo-item--newclient">
  <img src="images/logos/newclient.svg" alt="クライアント名" />
</div>
```

- `works-logo-item--newclient` の `newclient` は他と重複しない好きな名前
- `alt="..."` には正式名称（読み上げ・SEO用）

### 並び順について（重要）
- PCでの表示順 ＝ HTMLに書いた順番です
- タブレット／スマホは `style.css` の `@media` 内で `order` 番号により並びを指定しています
- **デバイスごとに違う並びにしたい場合**は、`style.css` の「Tablet (3-col)…」「Mobile (2-col)…」のブロックに、追加ロゴの `order` を足してください（不要なら省略可。その場合は末尾に並びます）
- `www.sib.tv`（シブヤテレビジョン）は常に最下段中央に出る設計です。新ロゴは原則それより前に追加してください

### ロゴの大きさを微調整したい
`style.css` の「Optical size balancing」ブロックで、ロゴごとに最大サイズを指定できます。

```css
.works-logo-item--newclient img { max-height: 30px; }      /* 高さ上限 */
.works-logo-item--newclient img { max-width: min(160px, 100%); } /* 横幅上限（横長ロゴ向け） */
```

- 大きすぎる → `max-height` を小さく（例 24px）
- 小さすぎる（縦に詰まったロゴ） → `max-height` を大きく（例 36px）

---

## 2. リンク（Connect）を変更する

`index.html` の `<section id="connect">` 内、各 `<a href="...">` の URL を書き換えます。

```html
<a class="connect-item" href="https://example.com/..." target="_blank" rel="noopener">
  <span class="connect-name" data-en="表示名(英)" data-jp="表示名(日)">表示名</span>
</a>
```

- `href` … リンク先URL
- `data-en` / `data-jp` … 英語/日本語表示名

---

## 3. プロフィール本文（About）を変更する

`index.html` 内：
- 日本語本文 … `<div class="bio jp ..." id="bio-jp">` の中
- 英語本文 … `<div class="bio en" id="bio-en">` の中

JP / EN の切り替えボタンはページ側に実装済みです。

---

## 4. 写真を差し替える

`images/` フォルダ内の該当画像を、同じファイル名で上書きするのが簡単です。
- ヒーロー（トップ大画像）… `images/`（`index.html` の `#hero` 内 `<img>` を参照）
- ギャラリー … `images/dj-portrait.jpg` / `images/tokyo-node.png`
- SNS共有時のサムネ（OGP画像）… `images/lisa-photo-web.jpg`（差し替え時は `index.html` の `og:image` も確認）

ファイル名を変える場合は、`index.html` 内の `src` も合わせて変更してください。

---

## 5. 変更を公開する（反映）

GitHub上でファイルを編集してコミットするか、ローカルで編集して push します。
`main` への反映後、1〜2分で本番に自動公開されます。

公開状況の確認は、同梱の `./check-deploy.sh` か、
GitHub の Actions / Vercel ダッシュボードで確認できます。

---

## 困ったとき
- 表示が崩れた場合は、編集前の状態に戻せます（GitHubのコミット履歴から復元可能）
- 不明点は仕様書 `SPEC.md` / `仕様書.md` を参照してください
